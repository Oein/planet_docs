# 반복문

반복문 `while`의 경우 `if`와 같은 형태로 작성합니다.

::::code-group

```js [while.planet]
var a = 0;
while (a < 10) {
  print(a);
  a = a + 1;
  if (a == 5) {
    break;
  }
}
```

```py [결과]
0
1
2
3
4
```

::::

`break`로 반복문을 조기 종료할 수 있습니다.
`continue`와 `for`은 지원되지 않습니다.
