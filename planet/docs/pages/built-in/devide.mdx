# devide(int|float|double var, int|float|double var2)

`var / var2`를 소수점 아래 5자리까지 계산합니다.
반환값의 타입은 `float`입니다.

`divide`가 아닌 `devide`로 정의되어 있습니다. 오타에 주의합시다.

```python
print(devide(1, 2)) # 0.5
print(devide(1.5, 2.5)) # 0.6
print(devide(1.123, 2.123)) # 0.52896845
```
