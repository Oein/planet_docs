# multiply(int|float|double var, int|float|double var2)

`var * var2`를 소수점 아래 5자리까지 계산합니다.
반환값의 타입은 `float`입니다.

```python
print(multiply(1, 2)) # 2.0
print(multiply(1.5, 2.5)) # 3.75
print(multiply(1.123, 2.123)) # 2.384129
```
