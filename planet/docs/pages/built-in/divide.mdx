# divide(int|float|double var, int|float|double var2)

`var / var2`를 소수점 아래 5자리까지 계산합니다.
반환값의 타입은 `float`입니다.

```python
print(divide(1, 2)) # 0.5
print(divide(1.5, 2.5)) # 0.6
print(divide(1.123, 2.123)) # 0.52896845
```
